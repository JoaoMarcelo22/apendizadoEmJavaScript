/*
    Boolean
    Null
    Undefined
    Number
    String
    Symbol
*/

// object - chave e valor

const x = {
    nome: 'renan',
    idade: 30,
    falar: function(){
        console.log(`meu nome ${this.nome}`) // fnções pode ser passada como parametros
    }
}

x['nome'] // atribuir

x.falar(); // recuperar

// +Infinity -Infinity e NaN

const y = pessoa.falar();

y();